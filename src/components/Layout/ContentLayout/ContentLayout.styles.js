import { styled } from "@mui/material";

export const StyledContentLayout = styled("div")(({ theme }) => ({
  width: "100%",
  height: "100vh",
  backgroundColor: theme.palette.background.default,
}));
